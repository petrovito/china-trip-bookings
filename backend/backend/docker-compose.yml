version: "3.8"
services:
  api:
    build: .
    ports:
      - "8080:8080"
    environment:
      - DATABASE_URL=${DATABASE_URL}
      - WRITE_PASSWORD=${WRITE_PASSWORD}
      - UNSPLASH_ACCESS_KEY=${UNSPLASH_ACCESS_KEY}
